export enum GatewayStatus {
  ACTIVE = 'active',
  REVOKED = 'revoked',
}

export enum GatewayLiveStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  UNREACHABLE = 'unreachable',
  UNKNOWN = 'unknown',
}
